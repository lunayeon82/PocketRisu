# PocketRisu 프로젝트 컨텍스트

RisuAI 포크. upstream 자동 머지 안 함. 필요 시 src/ts 변경분만 수동 cherry-pick.

## 배포 환경
- AWS Lightsail (서울, 1GB RAM, Ubuntu 24.04)
- Nginx 리버스 프록시 (서브도메인 risu.lunayeon.com → PM2 앱)
- PM2 프로세스 관리 (/usr/bin/pm2)
- SQLite: better-sqlite3, ~/oc-yeonsung/server/data/shared.db
  - 다른 앱(oc-yeonsung)과 공유, 테이블 접두사 rl_
- R2: @aws-sdk/client-s3, img.lunayeon.com
- GitHub Actions 자동 배포 (push → SSH → git pull → pnpm build → pm2 restart)

## 아키텍처
- 3-엔트리 반MPA: index.html, settings.html, chat.html (각각 별도 Vite 엔트리)
- 각 엔트리 안에서는 전역 Svelte store로 뷰 스위칭 (라우터 없음)
- 로컬 퍼스트: 채팅 데이터는 각 사용자 브라우저 IndexedDB에 저장
- 서버 저장 모드: 채팅을 placeholder로 부팅 → 처음 열 때 hydration
- 캐릭터 카드/프리셋/설정(=database.bin 전체) 계정별 완전 분리 완료 (아래 "계정별 kv/에셋 분리" 항목 참고) — 채팅·채팅폴더(rl_chats/rl_chat_folders)·공유 로어북(rl_lorebooks 계열)에 이어 database.bin도 계정별로 나뉨. `checkAuth`는 실제로는 rl_auth 세션(`getSession`) 그대로 사용 — "RisuAI 자체 JWT로 rl_auth와 무관"이라던 예전 설명은 stale였음

## 완료된 작업
- 계정별 kv/에셋 분리 — database.bin(캐릭터/프리셋/설정)을 채팅·채팅폴더 수준으로 계정별 완전 분리
  - 스코핑 방식: `server.cjs`에 `scopedKey(userId, key) = 'u/<userId>/<key>'` 도입, kv 저장소를 거치는 모든 계정 데이터(database.bin, 그 백업 스냅샷 `database/dbbackup-*`, `assets/*`, `remotes/*`, `coldstorage/*`, `drafts/*`, `migration-backup/*`, REMOTE-block 마이그레이션 마커)에 적용. `/api/read|write|patch|list|remove|assets/bulk-read|assets/bulk-write` 같은 범용 엔드포인트는 클라이언트가 어떤 키를 쓰든 예외 없이 무조건 스코핑(화이트리스트 방식 아님) — `/api/list` 응답은 `unscopedKey`로 되돌려서 클라이언트는 계정 개념을 몰라도 됨. `config/*`(스냅샷 보관 개수/용량, 서버 백업 저장 경로, 부팅 리마인더)는 의도적으로 전역 유지 — 서버 운영 설정이지 유저 데이터가 아님. `/api/logs`(별도 SQLite, 계정 개념 자체 없음)와 rl_chats/rl_chat_folders/rl_lorebooks* 계열은 이미 올바르게 분리/공유되어 있어서 전혀 안 건드림
  - **모듈 전역 인메모리 상태도 계정별로 전환 필요했음**(빠뜨리면 계정 간 저장이 섞이는 실제 데이터 손상 버그로 이어짐): `dbCache`/`saveTimers`는 키를 `dbHexKeyFor(userId)`(스코핑된 키의 hex)로 바꿔서 구조 변경 없이 계정별 격리, `dbEtag`/`lastPersistFailure`/`lastBackupTime`은 전부 `Map<userId, ...>`로 전환. `decodeDatabaseWithPersistentChatIds`/`createBackupAndRotate`/`flushPendingDb`/`invalidateDbCache`/`migrateRemoteBlocksIfNeeded`/콜드스토리지 복원 함수들/`importBackupFromSource`까지 전부 `userId`를 첫 인자로 받도록 시그니처 변경
  - **캐릭터 이미지(inlay 에셋)**: 기존 `save/inlays/<id>.<ext>`(전역 평면 디렉토리) → `save/inlays/<userId>/<id>.<ext>`로 계정별 서브디렉토리 분리. 백업 임포트 중 스테이징 디렉토리도 계정별(`inlays_import_staging_<userId>`)로 분리해서 동시 임포트 충돌 방지, 최종 스왑도 원자적(rename)으로 계정별 디렉토리에 반영
  - **`/api/asset/:hexKey`에 있던 실제 보안 구멍 수정**: 이 엔드포인트가 rl_auth와 무관한 별도 `sessionAuthMiddleware`(risu-session 쿠키)로만 게이트되어 있어서 계정 스코핑이 아예 없었음 — `resolveUserId(req)` 추가해서 두 체크를 병행(세션 쿠키 유효성 + rl_auth 계정 스코핑)
  - **서버 백업 파일/admin force-restore도 계정별 분리**: `backupsDir/<userId>/risu-backup-*.bin`. `/api/admin/force-restore`(ADMIN_RESTORE_TOKEN, rl_auth 우회하는 재해복구용 엔드포인트)는 세션이 없어 대상 계정을 추론할 수 없으므로 `X-Target-Username` 헤더/`?username=` 쿼리로 명시 요구(없거나 존재하지 않는 계정이면 400, 절대 admin으로 암묵적 fallback 안 함)
  - **복원/임포트 계열은 이제 "내 계정 데이터만" 지우고 복원**: `/api/backup/import`, `/api/migrate/save-folder/*`, `/api/db/snapshots/restore` 전부 예전엔 배포 전체를 `kvDelPrefix`로 지우고 복원했는데(계정 분리 전엔 그게 유일한 데이터였으니 맞았지만), 계정별로 나뉜 지금은 그대로 두면 계정 B가 백업을 복원하면 계정 A 데이터까지 날아가는 버그가 됨 — 호출하는 계정의 `u/<userId>/*` 네임스페이스만 지우고 복원하도록 수정
  - **`activeSessionId` 단일-writer 423 락도 계정별로 전환**(구현 중 별도로 발견): 원래 서버 전체에 스칼라 변수 하나(`activeSessionId`)로 "마지막으로 /api/session 부른 탭이 유일한 writer"였는데, 계정 분리 전엔 원래 단일 유저용 기능이라 문제 없었지만 계정이 여러 개가 된 지금은 계정 A가 새 탭을 열면 계정 B의 쓰기가 전부 423으로 튕겨나가는 진짜 크로스-계정 간섭 버그가 됨 — `activeSessionIdByUser: Map<userId, sessionId>`로 전환, 모든 `checkActiveSession(req, res)` 호출부(23곳)를 `checkActiveSession(req, res, resolveUserId(req))`로 스코핑. CLAUDE.md에 이미 기록된 "chatApi.cjs엔 이 락을 적용하면 안 됨"과 정확히 같은 계열의 문제였음
  - **구글 드라이브 백업 기능 완전 삭제**(`server/node/gdrive-backup.cjs`, `/api/gdrive/*`, `SystemBackup.svelte`의 관련 UI) — 이미 작동 안 하고 있었고, 계정별 백업 정책은 추후 별도 작업 예정([[gdrive_backup_deprecation]] 메모리 참고)
  - **1회성 마이그레이션**(`migratePerAccountData()`, `startServer()`에서 `bootstrapAdmin()`→`migrateInlaysToFilesystem()` 다음 실행, 전역 마커 `migration/per-account-kv-split-done`로 딱 한 번만): `rl_users`에서 `is_admin=1`이 정확히 1명일 때만 진행(0명 또는 2명 이상이면 로그만 남기고 스킵, 다음 부팅에 재시도) — 기존에 공유되어 있던 모든 계정 데이터(kv 키 + 최상위 inlay 파일)를 그 관리자 계정으로 이관, 다른 계정은 빈 상태로 시작(당시 실사용 계정 없었음, 의도된 동작)
  - **검증**: 가짜 "기존 공유 데이터"(제대로 인코딩된 database.bin 포함)를 마이그레이션 전 상태로 시드 → 부팅 → 마이그레이션이 정확히 관리자 계정(`u/1/...`)으로 이관했는지 SQLite 직접 조회로 확인. 관리자/신규 계정(userB) 두 계정으로 실제 로그인해서 database.bin/에셋/inlay 이미지/서버 백업 목록 전부 교차 계정 유출 없음을 `/api/read`·`/api/list`·`/api/asset/:hexKey`·`/api/backup/server/list`로 직접 확인, `activeSessionId` 수정도 계정 A의 탭 전환이 계정 B의 쓰기를 423으로 막지 않는 것 실제 재현 테스트로 확인. 채팅 생성/조회(건드리지 않은 코드)도 정상 동작 확인
- 코드 스플리팅: Monaco→dynamic import, wasmoon→lazy load, Settings→lazy load, katex/highlight.js→manualChunks 분리
- 채팅 전환 깜빡임 개선: LoadingOverlay→fade 트랜지션, BackgroundDom→캐릭터ID 키 추가, DefaultChatScreen→분기별 transition
- 백업 업로드 수정: Nginx proxy_buffering/proxy_request_buffering off 적용
- 3단계: 서버 인증 게이트 (rl_users, bcrypt, rl_auth 세션 쿠키, authGate.cjs)
- 5단계: 채팅 폴더 UI 개선 (옵시디언 스타일) — chatTree.ts로 폴더/채팅 트리 구성, ChatTreeItem.svelte로 재귀 렌더링(접기/펼치기, 이름변경, 색상, 삭제 시 자식 승격), MoveToFolderModal.svelte로 컨텍스트 메뉴 이동, 드래그 앤 드롭(같은 종류끼리 순서 변경, 폴더로 이동, 최상위 드롭존, 최대 깊이 2 및 자기 자신 하위 이동 가드) — reorderChats/updateChatFolder API 사용
- 단일 캐릭터 자동 진입 (autoOpenSingleCharacter 설정) — 켜면 부팅 시 캐릭터 갤러리 건너뛰고 첫 번째(휴지통 제외) 캐릭터 채팅으로 바로 진입, 사이드바 캐릭터 추가(+) 버튼도 숨김. bootstrap.ts에서 처리
- 사이드바 "최근에 본" 목록 버그 수정 — Sidebar.svelte의 recentChars가 trashTime(휴지통) 필터링을 안 해서, 삭제(소프트 삭제) 캐릭터가 메인 그리드에서는 사라져도 최근 목록엔 3일 뒤 자동 정리 전까지 계속 표시되던 문제
- 4단계: 공유 로어북 저장소 (lorebookApi.cjs) + 캐릭터 globalLore 연결 — 현재 상태:
  - 서버 스코프/잠금/버전 로직: rl_lorebooks.scope('global'/'private')+owner_id. global은 전체 공개, private은 owner_id 본인만 조회/편집(canView()로 404 처리). private→global(to-global)은 단방향. global→private 복제(clone)는 entry id 전체 재발급으로 원본과 완전 분리. 삭제는 global=관리자만/private=소유자만. 잠금은 책이 아니라 **항목(entry) 단위**(rl_lorebook_locks/rl_lorebook_drafts) — **만료 없음**(비관적 락, 같은 유저 재요청은 갱신, 다른 유저는 저장/취소 전까지 계속 409). 실제 내용 편집(PUT .../entries/:entryId)만 잠금 필요, 추가/삭제/순서변경/이름변경은 잠금 불필요. 버전 스냅샷(최근 3개, rl_lorebook_versions)은 책 전체 단위, 잠금 무관. entry_id는 모든 쓰기 경로에서 backfillEntryIds()로 채움. rl_lorebook_overrides 테이블은 서버에 남아있지만 **클라이언트에서 더 이상 안 씀**(죽은 경로) — 활성화 모드는 이제 순전히 로컬(아래 참고)
  - 별도 목록/편집 화면(SharedLoreBookStore.svelte, lorebookDraftDb.ts)은 삭제됨 — 공유 저장소는 순수 "발행/구독" 채널이고, 편집은 항상 캐릭터 자신의 로어북 화면(LoreBookData.svelte)에서만 함
  - 연결: loreBook에 source_lorebook_id/source_updated_at 추가. 로어 항목 1개 = 공유 로어북 1개(entry-level, content:[그 항목]) 대응. sharedLorebookLink.svelte.ts가 업로드(uploadEntryToSharedLorebook, 잠금→저장)/동기화(syncEntriesFromSharedLorebook, 받아오기·업데이트·버전복원이 공용으로 씀)를 담당 — 동기화는 콘텐츠는 서버 값으로 교체하되 alwaysActive/disabled(활성화 방식)는 항상 로컬 값을 보존
  - 백그라운드 동기화: LoreBookSetting.svelte가 Character 탭이 열려있는 동안 15초 폴링(startBackgroundSync/stopBackgroundSync)으로 "아직 안 가져온 새 글로벌 책"/"연결된 항목의 원본이 갱신됨"을 감지해 로어북 화면 상단에 배너("받아오기" 버튼 하나)로 표시, 클릭 시 일괄 반영하고 결과(신규 n건/업데이트 n건)를 토스트로 알림
  - **[업데이트: 계정별 kv 분리 이후로 stale] character.globalLore는 원래 계정별로 분리되지 않았다** — database.bin 전체가 배포 공유이던 시절 얘기. "계정별 kv/에셋 분리" 항목대로 지금은 database.bin이 계정별로 나뉘어서, `value`에 직접 쓴 WIP이 broadcast되는 범위는 "배포 전체"가 아니라 "그 계정 자신"으로 좁아짐. 다만 아래 잠금/editDraft 설계 자체(공유 로어북 저장소는 여러 계정이 동시에 편집할 수 있는 공용 공간이라 별도 잠금이 필요하다는 것)는 여전히 유효하고 바뀐 거 없음 — 그래서 이 항목 로직은 손 안 댐. 편집: source_lorebook_id가 있는 항목은 LoreBookData.svelte에서 기본 읽기전용 — "편집 시작"(pencil)이 서버 잠금(POST .../lock, rl_lorebook_locks, entry 단위, **TTL 없음**·비관적 락)을 쥐어야만 편집 가능해지는데, 이때 실제 입력은 `value`가 아니라 별도 로컬 상태 `editDraft`(LoreBookData.svelte)에 반영됨 — `value`는 항상 마지막으로 알려진 canonical 콘텐츠만 들고 있음. `editDraft`는 잠금 응답이 준 내용(canonical 사본 또는 이 유저 본인이 예전에 남긴 draft)으로 시작하고, 타이핑하는 동안 600ms 디바운스로 `PUT .../entries/:entryId/draft`(rl_lorebook_drafts, **유저별로 진짜 분리됨**)에 계속 저장되어 새로고침해도 "편집 시작"을 다시 누르면 이어서 편집 가능. **잠금은 단일 소유자 배타적**(I/O 독점과 동일 원리)이라 한 명이 쥐고 있는 동안 다른 사람은 편집 시작 자체가 409로 막힘. 잠금 해제는 "업로드" 성공(서버 saveEntry가 draft+lock 자동 삭제) 또는 "편집 취소"(DELETE .../lock, **draft도 같이 삭제되므로 작성 중이던 내용은 사라짐** — 확인창 있음) 둘 중 하나뿐. "업로드" 버튼은 연결 안 된 항목(신규 등록)이거나 지금 편집 중(editingShared)인 연결된 항목에만 뜸 — 업로드 시 확인창("기존 공유 로어북 'OO'을 덮어씁니다") 후 uploadEntryToSharedLorebook(target, content, existing)이 content(=편집 중이면 editDraft, 아니면 value 자신)를 업로드하고 잠금 획득+저장을 원자적으로 수행, 성공 시 그 내용을 target(=value)에도 복사해 업로더 본인 화면에도 즉시 반영. "공유" 뱃지 옆 "미업로드" 뱃지는 editingShared 여부로만 표시(콘텐츠 diff 비교 없음 — value가 절대 안 바뀌므로 필요 없음). 받아오기(syncEntriesFromSharedLorebook)는 이제 항상 무조건 덮어써도 안전 — value가 WIP을 들고 있는 경우가 없으므로 스킵/force 로직 불필요. 버전 기록/복원 버튼은 여기 있음
  - 활성화 방식은 3택(상시/키워드트리거/비활성) — loreBook.disabled 필드 추가, loadLoreBookV3Prompt에서 disabled 항목은 프롬프트 조립에서 제외. 순전히 개인 설정이라 서버로 안 올라가고(동기화 시 보존됨, 위 참고) 항상 로컬에서만 관리
  - 외부 JSON 재import(importLoreBook, risu 포맷)는 source_lorebook_id/source_updated_at를 항상 벗겨내고 개인 상태로 저장 — 공유 링크가 파일 경유로 의도치 않게 따라붙는 것 방지
  - 미구현: 항목 순서 변경 API(PATCH .../entries/reorder)는 서버에 있지만 드래그 UI는 아직 없음
  - **버그 수정: 공유 로어북 받아오기가 항목을 안 보이게 만들던 문제** — 계정별 kv 분리 작업 직후 발견됨(원인은 그 작업과 무관, 원래부터 있던 버그가 처음으로 다른 계정에서 노출된 것). 업로드(`uploadEntryToSharedLorebook`)가 항목의 로컬 `folder` 키를 같이 서버에 저장했고, 받아오기(`syncEntriesFromSharedLorebook`)가 그 값을 그대로 물려받았음 — 받는 캐릭터엔 그런 폴더가 없으니 `LoreBookList.svelte`의 폴더별 필터 렌더링이 그 항목을 화면 어디에도 안 그림(globalLore 배열엔 실제로 들어있어서 "새 로어북 생성" 이름 카운터엔 잡히는데 목록엔 안 보이는 형태로 드러남). 수정: `folder`도 `alwaysActive`/`disabled`처럼 "개인 설정"으로 취급 — 업로드 시 안 보내고(`stripLinkMeta`), 받아올 때도 서버 콘텐츠의 folder는 항상 무시하고 이미 로컬에 연결되어 있던 경우만 로컬 folder 값을 유지, 신규 수신 항목은 항상 최상위. 이미 발행된 기존 공유 로어북(folder 값이 이미 박혀있는 것)도 서버 데이터를 안 고쳐도 됨 — 받는 쪽이 이제 무조건 무시하므로 재동기화만 하면 해결됨
- 채팅 저장 동시성 보호 — PUT /api/chats/:id/full(upsertChatFull)이 버전 체크 없이 그 채팅의 메시지를 통째로 delete+reinsert하는 구조라, 같은 계정을 두 군데(탭 2개/PC+휴대폰)에서 동시에 같은 채팅에 저장하면 나중 요청이 먼저 요청의 메시지를 조용히 지울 수 있었음. body에 optional `expected_updated_at`을 추가해 기존 행의 updated_at과 다르면 409(덮어쓰지 않음)로 거부 — 클라이언트(nodeStorage.ts)는 chatUpdatedAtCache(모듈 전역 Map)로 채팅별 마지막 known updated_at을 들고 있다가 매 저장 시 실어보내고, 서버 응답/409의 updated_at으로 갱신. 충돌 시 saveChatToServer가 ChatConflictError를 던지고, globalApi.svelte.ts의 저장 루프는 그 채팅 저장만 건너뜀(로컬 메모리는 안 건드림 — 다음 실제 편집 때 다시 시도됨) — 절대 "누구 걸 지울지 자동으로 고르지" 않음. **의도적으로 안 한 것**: server.cjs의 activeSessionId 단일-writer 423 락(database.bin용)을 chatApi.cjs에도 적용하는 건 잘못된 방향 — 그건 배포 전체에 "한 세션만 쓰기 허용"이라 계정이 다른 두 유저가 각자 자기 채팅만 써도 서로를 423으로 튕겨냄(채팅은 원래 계정별로 완전히 분리돼 있는데 그 격리를 오히려 깨버림). 좁은 동시-편집 레이스에는 위 버전체크가 맞는 해법.
- 채팅/폴더 트리 UX 개선 — 이름변경을 인라인 입력 대신 ShDialog 모달로 분리(행이 draggable이라 인라인 입력 드래그 시 텍스트 선택이 행 드래그로 오인식되던 문제 해결), 뎁스별 세로 가이드라인으로 폴더 소속 명시, 드래그 중 "어디로 이동하는지" 설명 배너 추가, 최상위 드롭존을 드래그 중에만 노출되는 라벨 있는 큰 영역으로 개선, ondragend로 드롭존 상태 항상 리셋
- 채팅/폴더 트리 grip 핸들 추가 (ChatTreeItem.svelte/SideChatList.svelte) — 터치 기기 드래그 미지원 문제 해결
  - 각 행 왼쪽에 GripVerticalIcon 핸들: PC는 hover 시에만 보임(`group-hover:opacity-100`), 터치는 항상 보임
  - PC: `draggable`은 여전히 행 전체에 걸려있음 — 핸들 하나에만 `draggable`을 걸면 일부 브라우저 엔진에서 네이티브 `dragstart` 자체가 안 터지는 문제가 있어서 포기(실측: dispatchEvent로 강제 발동하면 정상 동작 → 핸들러는 멀쩡, 브라우저가 안 쏘는 것). 대신 행에 `onmousedown`을 걸어 눌린 지점이 핸들(`data-drag-handle`) 안인지를 `dragOriginatedOnHandle` 플래그로 기록해두고, `ondragstart`에서는 그 플래그만 검사해서 핸들 밖에서 시작된 드래그는 `preventDefault()`로 취소. **`ondragstart` 시점의 `e.target`으로 직접 검사하면 안 됨** — 드래그가 draggable 조상의 자식에서 시작돼도 네이티브 `dragstart`의 `e.target`은 실제로 눌린 요소가 아니라 draggable 조상 자신(행)으로 귀결되므로, `e.target.closest('[data-drag-handle]')`는 자식 방향인 핸들을 절대 못 찾음(`closest`는 조상 방향만 훑음) — 반드시 `mousedown` 시점(그때는 `e.target`이 진짜 눌린 요소)에 판정해서 플래그로 넘겨야 함
  - 터치: 핸들의 `pointerdown`에서 300ms longpress 판정(10px 초과 이동 시 스크롤로 간주해 취소) → `setPointerCapture`로 이후 이벤트를 핸들에 고정 → `pointermove`마다 `document.elementFromPoint`+`data-chat-tree-row`/`data-chat-tree-root-drop` 속성으로 직접 hit-test(`resolveDropTargetAtPoint`, 네이티브 dragover가 없어서 직접 구현) → `pointerup`에서 기존 `onDropOn`/`onDropRoot`를 그대로 호출. `touch-action: none`(Tailwind `touch-none`)은 핸들에 항상 거는 게 아니라 `isTouchDevice`일 때만 — PC에서까지 걸려있으면 네이티브 드래그 미발동의 원인 후보 중 하나였음(다른 원인으로 확인됐지만 안전하게 조건부로 유지)
  - 드롭존 하이라이트(`dropZone`)는 각 행이 로컬 `$state`로 들고 있던 걸 SideChatList의 공유 `hoverTarget` state에서 `$derived`로 파생하는 방식으로 전환 — 터치 드래그는 시작한 행과 다른 행 위를 hover할 수 있어서(포인터 캡처가 다른 인스턴스의 상태를 못 건드림) 공유 state가 필요했음. 네이티브 dragover와 터치 hit-test 둘 다 같은 `onHoverChange`/`onHoverClear` 콜백을 거치므로 하이라이트 로직은 하나만 존재
  - 기존 `onDropOn`/`onDropRoot`/`reorderChats`/`persistChatOrder`/깊이 가드 로직은 전혀 안 건드림 — PC 네이티브 드래그와 터치 커스텀 드래그가 최종적으로 같은 함수를 호출하도록만 연결
- 채팅 순서·폴더 소속 버그 2건 수정 (chatApi.cjs, nodeStorage.ts)
  - 새 채팅이 화면에선 맨 위인데 재접속하면 맨 아래로 밀리던 문제 — 새 채팅 생성 코드들(6곳)이 로컬 배열엔 `unshift`로 맨 앞에 넣지만, 서버(`upsertChatFull`의 INSERT 분기)는 `nextPosition()`(= 기존 채팅 중 최댓값+1, 즉 맨 뒤)으로 저장하고 있었음 → position=0으로 삽입하고 같은 스코프(user_id+character_id+folder_id)의 기존 행들 position을 전부 +1 시프트하도록 수정. 생성 코드 6곳은 안 건드림(서버만 클라이언트 쪽 unshift 가정에 맞춤)
  - 드래그로 폴더 이동한 채팅이 재로드 후 가끔 폴더 밖으로 빠져나오던 문제 — `folder_id`가 서버에 컬럼과 `chat_meta` JSON 두 군데에 중복 저장되는데, 드래그 이동(`PATCH /reorder`, `/meta`)은 컬럼만 갱신하고 `chat_meta`는 그대로 둠. 채팅을 다시 열 때(`fetchChatContent`)는 `chat_meta.folderId`(stale할 수 있음)만 읽고 있었어서, 열람 시점에 옛 폴더 값으로 되돌아갔다가 다음 자동저장에 그 값이 다시 영구 반영되던 것 — 응답 최상위의 `folder_id` 컬럼(항상 최신)을 우선하도록 수정
- 채팅 트리 grip 핸들 — PC 네이티브 드래그가 아예 안 먹던 버그 + 그 수정이 만든 터치 회귀 + 재발한 폴더/순서 리셋 버그, 3건 연쇄 수정
  - **PC: grab 커서는 뜨는데 실제 드래그가 시작 안 됨** — `dragstart`→`dragend`가 5ms 만에 연속 발화하고 그 사이에 `dragover`가 단 한 번도 안 뜨는 걸 이벤트 리스너로 확인(`document.querySelectorAll('[data-chat-tree-row]')`의 Y좌표를 dragstart/dragend 시점마다 찍어봄). 원인: `SideChatList.svelte`의 `onDragStart()`가 `isDragging = true`를 동기적으로 세팅 → `{#if isDragging}` 배너 `<div>`가 리스트 컨테이너 **위에 in-flow로** 마운트되며 아래 행들이 28px 밀려 내려감 → 네이티브 HTML5 DnD는 드래그 소스의 레이아웃 박스가 dragstart 직후 움직이면 그 자리에서 드래그를 취소하는 스펙이라, Chrome이 `dragover` 한 번 못 띄우고 드래그를 죽여버림(개발자도구에는 아무 에러도 안 남아서 원인 추적이 까다로움) — grip 핸들 커밋과 "드래그 중 설명 배너" 커밋이 시점이 겹치며 생긴 회귀로 추정. 수정: 배너를 리스트 위 zero-height `<div class="relative">` 앵커 안에 `absolute` + `z-10`으로 오버레이시켜서, 마운트/언마운트가 리스트의 실제 레이아웃을 절대 안 건드리게 함
  - **위 수정이 만든 터치 회귀** — 배너가 이제 `z-10`으로 리스트 맨 위 행 위에 시각적으로 겹치는데, 터치 드래그(`resolveDropTargetAtPoint`)는 네이티브 `dragover`가 없어서 `document.elementFromPoint(x, y)`로 손가락 아래 요소를 직접 hit-test함 — 배너가 그 좌표에서 실제 행보다 위에 있으니 hit-test가 배너 자신을 집어서 `.closest('[data-chat-tree-row]')`가 null이 되고, 리스트 맨 위 근처로 드롭할 때만 조용히 아무 일도 안 일어남(에러도 안 남음). 배너에 `pointer-events-none` 추가해서 hit-test를 통과하도록 수정 — 배너는 순수 정보 표시용이라 애초에 hit 대상이 될 이유가 없었음
  - **재발한 "새로고침하면 채팅 위치/폴더가 원래대로 되돌아간다" 버그** — 5단계 때 고쳤던 클라이언트 읽기 경로(`fetchChatContent`가 `chat_meta.folderId` 대신 top-level `folder_id` 컬럼을 우선하도록 한 것) 자체는 여전히 멀쩡했음(boot/`loadChatListFromServer`도 전부 컬럼 소스 확인됨). 실제 원인은 **쓰기 경로의 레이스**: `upsertChatFull`(전체 채팅 자동저장, `PUT .../full`)이 기존 채팅을 업데이트할 때마다 클라이언트가 보낸 `chat_meta.folderId`로 `folder_id` 컬럼을 매번 덮어썼음. 근데 드래그 정렬/폴더 이동이 쓰는 `reorderChats`(`PATCH /reorder`)와 `patchChatMeta`(`PATCH .../meta`)는 **의도적으로 `updated_at`을 안 건드림**(정렬 좀 했다고 최근순 정렬에서 맨 위로 튀는 게 이상해서) — 그래서 `upsertChatFull`의 낙관적 동시성 체크(`expected_updated_at`)가 "그 사이에 리오더가 있었다"는 걸 절대 감지 못 함. 메시지 많은 채팅이라 자동저장 요청이 느리게 나가는 동안(특히 모바일 네트워크) 사용자가 드래그로 폴더 이동을 하면: 리오더가 먼저 도착해서 컬럼을 정확히 갱신 → 뒤늦게 도착한 자동저장이 드래그 이전 시점의 `chat_meta.folderId`로 다시 컬럼을 덮어씀 → 새로고침하면 아까 그 되돌려진 값이 그대로 보임. 수정: `updateChat`/`upsertChatFull`은 **기존 채팅에 한해** `chat_meta.folderId`를 절대 안 읽고 DB에 있던 `folder_id`를 그대로 보존하도록 변경 — 폴더 배정은 이제 전적으로 `reorderChats`/`patchChatMeta` 전담. 새 채팅 생성(INSERT 분기)만 예외적으로 여전히 `chat_meta.folderId`를 씀(가져오기·브랜치처럼 처음부터 폴더 안에 만드는 게 정당한 유일한 케이스라서)
  - 위 수정의 부작용으로 `Chat.svelte`의 "브랜치" 기능(`createFolderOnBranch` 설정)이 깨질 뻔한 걸 발견 — 브랜치 시 새 폴더를 만드는 코드가 `createChatFolder` API를 아예 호출 안 하고 로컬 `chara.chatFolders`에만 push한 뒤 현재 채팅의 `folderId`를 로컬로만 바꾸고 다음 자동저장(`chat_meta`)에 얹혀 서버로 가길 기대하고 있었음 — 위 수정이 들어가면 그 자동저장이 더 이상 folder_id를 안 옮기니 폴더 배정이 영원히 서버에 안 감. `createChatFolder` + `updateChatMeta`를 명시적으로 호출하도록 수정
  - **검증**: 실제 레이스를 API 레벨로 직접 재현(리오더 먼저 도착 → 그 다음 구버전 `chat_meta.folderId`를 담은 `PUT .../full` 도착) → 수정 전이면 폴더 밖으로 튕겨났을 상황에서 폴더 안에 정상 유지되는 것 확인, 새로고침 후에도 유지
  - **[업데이트: stale, 실제로는 이미 고쳐짐] 여기 적혀있던 "아직 안 고친 것" 하위 커밋(`9a35c798`, "fix(chats): stop reorder batch from failing whole-cloth on a stale id")에서 이미 해결됨** — `reorderChats`(`chatApi.cjs`)가 더 이상 배치 전체를 all-or-nothing으로 404시키지 않고, `stmtOwnsChat`으로 소유 확인된 id만 걸러서(`owned`) 적용하고 나머지는 `skipped`로 응답에 담아 알려줌(`nodeStorage.ts`의 `reorderChats` 클라이언트 래퍼는 `skipped`가 있으면 콘솔 경고만 남김). 즉 드래그 정렬 배치에 아직 서버에 없는 새 채팅 id가 섞여 있어도 나머지 형제들의 순서는 정상 반영되고, 그 새 채팅만 이번 배치에서 조용히 스킵됨(다음 배치 때 다시 시도됨) — 원래 가설이었던 "배치 전체 404" 실패 모드는 현재 코드에서 재현 안 됨
- AI 스트리밍 응답 서버 상시 버퍼링 (durable generation buffering) — 서버는 상시 켜져있는데도 `/proxy2`가 완전 무상태 릴레이(`pipeline(originalResponse.body, res)`)라 클라이언트 연결이 끊기면(탭 백그라운드/네트워크 단절) 업스트림 fetch까지 같이 끊겨서 AI 응답이 통째로 유실되던 문제 해결. 로컬 LLM용으로 이미 있던 `/proxy-stream-jobs`(클라이언트 응답과 분리된 pump 루프, `server.cjs`)와 정확히 같은 발상을 클라우드 AI 릴레이에 적용하되 인메모리 대신 SQLite에 영속화
  - **서버**: 신규 `server/node/pendingGenApi.cjs` — `rl_pending_generations` 테이블(id=클라이언트 generationId, user_id, room_chat_id, character_id, parser_kind, replay_meta, status streaming/done/error, raw_body BLOB, byte_length, truncated, upstream_status, error_message, 타임스탬프들) + `GET /api/pending-generations?roomChatId=`(목록)/`GET /api/pending-generations/:id`(raw_body base64 포함 전체)/`DELETE /api/pending-generations/:id`(ack, 멱등) 세 엔드포인트. `resolveUserId`는 `chatApi.cjs`가 export하는 것 재사용(3중 구현 방지)
  - **`reverseProxyFunc`(`server.cjs`) 변경**: 클라이언트가 opt-in 헤더 `risu-durable-meta`(genId/roomChatId/characterId/parserKind/replayMeta, `risu-header`와 같은 URL-encoded JSON 관례)를 보낼 때만 새 `runDurableProxyPump` 분기를 탐 — 헤더 없으면 기존 `pipeline()` 경로 완전히 그대로라 이미지/백업/플러그인 fetch 등 나머지 `/proxy2` 트래픽은 동작/성능 변화 전혀 없음. 핵심 수정: `originalResponse.body.getReader()`로 직접 pump 루프를 돌려서 루프 지속 여부가 `res`(클라이언트 연결)에 전혀 안 묶이게 함 — `res.write()`는 best-effort(에러나면 `resWritable=false`만 세우고 루프는 계속), 동시에 인메모리 버퍼에 누적하며 스로틀(≥1초 또는 ≥8KB마다) SQLite flush, 상한 4MB(넘으면 truncated 표시하되 라이브 릴레이는 계속). 종료/에러 시 최종 flush + status 확정. GC는 `PROXY_STREAM_GC_INTERVAL_MS` 스윕과 같은 자리에 sibling `setInterval` — 10분 무갱신 `streaming` 행은 `error`로, 종료 상태 행은 12시간(`PENDING_GEN_TTL_MS`, env 조정 가능) TTL 삭제, 유저당 50개 보관 캡
  - **서버는 끝까지 원본 바이트만 저장, 파싱 안 함**: 클라이언트의 SSE 파서(`openAI/requests.ts`/`google.ts`의 `getTranStream()`, `anthropic.ts`의 파서)는 전부 원본 버퍼 전체를 통째로 넣어도 라이브와 동일한 최종 텍스트를 재현하는 replay-safe 구조(매 청크마다 누적 버퍼 전체를 재파싱하는 기존 구현 방식 덕에 우연히 replay-safe)였고, 새 adapter 경로(`preset/adapter/*.ts`)의 `parseSseStream()`도 마찬가지라서, 서버에 프로바이더별 파싱을 새로 구현/중복할 필요가 전혀 없었음
  - **클라이언트 배선**: `sendChat()`(`index.svelte.ts`)이 방 채팅id/캐릭터id로 `durable` 메타를 만들어 `requestChatData`에 실음 → `requestChatDataMain`(`request.ts`)이 실제 포맷(레거시 3종: openai/anthropic/google)이나 adapter kind(신규 3종)를 안 시점에만 `parserKind` 확정, 그 외 포맷(Kobold/NovelAI/Ollama/Plugin 등)은 `durable`을 아예 비워서 구조적으로 제외 — 오직 메인 어시스턴트 응답 스트리밍 호출 하나만 커버(감정 분류 등 보조 호출은 대상 아님). 실제 fetch 사이트(`openAI/requests.ts`/`anthropic.ts`/`google.ts`의 스트리밍 전용 호출들, 어댑터 경로는 `makeProxiedFetch`)까지 `durable` 필드를 넘겨 `fetchNative`→`fetchViaProxy2`(`globalApi.svelte.ts`)에서 헤더로 변환
  - **재생(replay)도 새 파서 없이 기존 라이브 파서 재사용**: `getTranStream()`/`getTranStream()`(google) export, `anthropic.ts`의 인라인 파서는 `buildAnthropicSseStream(reader, {replayMode})`로 추출(재생 중엔 라이브의 "overload 감지 시 재요청" 분기를 절대 타면 안 되므로 `replayMode`로 차단), 세 adapter 파일은 fetch와 뒤엉켜 있던 SSE 파싱 루프만 `mapXxxSseToDeltas(body)`로 분리 — 전부 라이브 호출부와 재생 호출부가 같은 함수를 공유. 신규 `src/ts/storage/pendingGenRecovery.ts`가 채팅 열람(`chatStorage.ts`의 `ensureChatHydrated`, placeholder 하이드레이션 완료 **후**에 체인 — 안 그러면 하이드레이션의 `chats[currentIndex]=full` 덮어쓰기와 레이스해서 복구 메시지가 유실될 수 있었음)/탭 포그라운드 복귀(`globalApi.svelte.ts`의 `visibilitychange`) 시 조용히 조회→재생→`message.chatId`로 중복 체크→반영→ack. `pendingGenRecovery.ts`는 결국 `request/*.ts`(→`globalApi.svelte.ts`)를 정적 import하므로 `globalApi.svelte.ts`/`chatStorage.ts`에서 거는 두 훅은 순환참조 방지를 위해 전부 동적 `import()`로 연결(실측: `vite build`가 `pendingGenRecovery`를 실제로 별도 청크로 분리해서 문제 없음을 확인)
  - **알려진 한계(의도적으로 미해결)**: (1) `fetchNative`가 직접 브라우저→프로바이더 fetch에 성공하는 CORS 허용 커스텀 엔드포인트는 `/proxy2` 자체를 안 타서 이 메커니즘 사정권 밖. (2) 두 탭이 비슷한 시점에 같은 채팅방에 각자 생성을 걸면 별도 generationId 2건이 orphan으로 남고 복구 시 둘 다 별개 메시지로 붙어 중복 응답처럼 보일 수 있음. (3) ModelPreset 툴콜 루프나 레거시 openAI/google의 툴콜 재요청은 같은 generationId로 여러 HTTP leg를 도는데, 서버 쪽 INSERT를 `INSERT OR REPLACE`로 처리해서 매 leg가 이전 leg를 덮어씀 — 즉 복구되는 건 항상 마지막 leg뿐, 툴콜 실행 자체는 재생 안 됨(재생은 순수 텍스트 재구성만 함)
  - **검증**: 스크래치 SQLite로 서버 실제 기동 후 로그인 → mock SSE 업스트림 상대로 `risu-durable-meta` 달아 `/proxy2` 실호출 → 버퍼링된 원본 바이트가 라이브 응답과 바이트 단위로 동일함을 확인. **핵심 시나리오**: curl을 150ms만에 강제 종료(mock은 400ms 걸려 완료)해도 서버 로그 상 pump가 계속 진행돼 전체 195바이트를 끝까지 `done`으로 완주하는 것 실측 확인(클라이언트 끊김이 업스트림을 안 끊는다는 핵심 수정이 실제로 동작). ack 멱등성(중복 DELETE도 204), 업스트림 fetch 자체가 즉시 실패하는 경우(durable 분기 진입 전) 기존 500 에러 경로가 그대로 유지되는 것, 인증 없는 요청 401 등 회귀 없음 확인. `svelte-check` 0 에러, `vite build` 정상 완료
- 새 채팅 여러 개 연달아 만들면 순서가 뒤섞이는 버그 수정 — 위 5단계 항목에 남아있던 "아직 안 고친 것" 메모(드래그 배치 404 가설)를 재조사하다가, 그 가설 자체는 이미 다른 커밋(`9a35c798`)에서 고쳐져 있었고(위 항목 업데이트 참고) 실제 원인은 별개라는 걸 발견
  - **진짜 원인**: 새 채팅 생성 6곳(`SideChatList.svelte`/`ChatTreeItem.svelte`/`Chat.svelte`(브랜치)/`ExcelSheetTabs.svelte`/`ChatList.svelte`, 전부 `chats.unshift(newChat)` 직후 `saveChatToServer(...).catch(()=>{})`로 fire-and-forget 즉시 저장)가 서로 아무 조율 없이 독립적인 `PUT /api/chats/:id/full` 요청을 날림. 서버 `upsertChatFull`의 신규 채팅 분기(`stmtShiftPositionsDown`으로 기존 행 전부 +1 시프트 → position 0에 insert)는 순수 "마지막에 커밋되는 요청이 0번을 차지"하는 구조라, 두 채팅을 연달아 만들면(더블클릭, 혹은 브랜치 직후 바로 새 채팅 등) 로컬에서 만든 순서와 무관하게 서버 트랜잭션이 늦게 끝난 쪽이 맨 위를 차지 — 새로고침하면 방금 만든 순서와 다르게 보임. 드래그가 전혀 없어도 재현되고, 오히려 이쪽이 훨씬 흔한 트리거
  - 백업/복원(`backuplocal.ts`)과 주기 자동저장 루프(`globalApi.svelte.ts`의 `persistTrackedChanges`)는 각자 `for` 루프 안에서 `await`로 이미 한 번에 하나씩 순차 저장하고 있어서 이 레이스와 무관 — 그대로 둠
  - **수정**: `chatStorage.ts`에 캐릭터별(`chaId`) 프로미스 체인 `saveNewChatToServer()` 추가 — 같은 캐릭터에 대한 "새 채팅 생성 직후 저장" 호출들만 이 큐를 거쳐 항상 로컬에서 만든 순서 그대로 서버에 도달하도록 직렬화(다른 캐릭터끼리는 서로 안 막음, 기존 `saveChatToServer`/주기 자동저장 경로는 안 건드림). 위 5곳의 fire-and-forget 호출을 전부 `saveNewChatToServer`로 교체
  - `svelte-check` 0 에러로 회귀 확인. 실제 동시-생성 재현 테스트(두 요청을 일부러 응답 속도 다르게 시뮬레이션 등)까지는 아직 안 함 — 다음에 실사용 중 재발 여부로 확인 필요
- 관리자 전용 깃 업데이트 알림 — `/admin` 페이지(`requireAdmin` 게이트, 기존)에서만 표시, 메인 앱 UI엔 안 건드림(사용자 확인 후 결정). 배포가 이 체크아웃 그대로 `git pull`이라(CLAUDE.md 배포 환경 참고) 로컬 HEAD와 origin 브랜치를 비교하는 것만으로 충분
  - `server.cjs`에 `getGitUpdateStatus()` 추가 — `git rev-parse --abbrev-ref HEAD` → `git fetch --quiet origin <branch>` → `git rev-parse HEAD`/`origin/<branch>` 비교, 다르면 `git rev-list --count HEAD..origin/<branch>`로 몇 커밋 뒤처졌는지까지 계산. **`execSync` 대신 async `exec`로 구현**(`execAsync` 헬퍼) — 단일 Node 이벤트루프짜리 서버에서 `execSync`로 네트워크 `git fetch`를 돌리면 느려지거나 멈췄을 때 다른 유저 요청까지 전부 막히므로, admin 페이지 조회 하나 때문에 서버 전체가 블로킹되면 안 됨(각 명령 5~8초 타임아웃도 걸어둠). `.git` 자체가 없는 배포(레포 밖에서 tarball로 배포하는 경우 등)는 `existsSync` 체크로 조용히 스킵. 실패 시(네트워크 문제 등) 배너 없이 조용히 넘어감(에러는 로그만)
  - `adminPageHtml`(`authGate.cjs`)에 `gitStatus` 파라미터 추가, `upToDate`가 아닐 때만 로컬/원격 커밋 앞 7자리 + 뒤처진 커밋 수를 담은 배너 렌더 — `escapeHtml`로 값 이스케이프
  - **검증**: 실제 서버 기동 후 로그인 → `/admin` 응답에서 배너 없음 확인(이 작업 자체를 커밋/푸시하기 전이라 로컬이 origin과 동일했음, 예상대로). `git rev-parse`/`fetch`/`rev-list --count` 커맨드 자체를 레포에서 직접 돌려 값 확인, `adminPageHtml`에 가짜 `gitStatus`(behindBy:3)를 직접 넣어 배너 HTML이 올바르게 렌더되는 것도 별도 확인
- 웹 푸시 알림(OS 레벨) 추가 — 지금까지 "응답 완료 알림"은 `index.svelte.ts`가 탭이 열려있을 때만 브라우저 `Notification` API를 쏘는 방식이라 탭을 닫거나 오래 백그라운드에 두면 안 떴음. durable buffering(위 항목)이 이미 "서버가 응답 완료 시점을 안다"는 조건을 만족시키므로 그 인프라를 재사용해서 진짜 Service Worker + Push API + VAPID 구현. 새 의존성 `web-push` 추가(pnpm)
  - **`public/sw.js`는 원래 있었지만 아무 데서도 등록된 적이 없어서 죽어있던 파일**(이미지 캐시/공유-타겟용, upstream RisuAI 유산) — `bootstrap.ts`의 `loadData()` 맨 앞에 `navigator.serviceWorker.register('/sw.js')`를 조건 없이(알림 설정과 무관하게) 추가해서 처음으로 활성화. 3개 엔트리(main/chat-main/settings-main)가 전부 `loadData()`를 그대로 호출해서 한 곳으로 다 커버됨. **부작용(의도된 것)**: 그동안 죽어있던 이미지 캐시/공유-타겟 핸들러도 같이 살아남
  - **VAPID 키**: `jwtSecret`/`instanceId`(`server.cjs:629-646`)와 정확히 같은 패턴으로 `save/__vapid_public_key`/`save/__vapid_private_key`에 최초 생성 후 영속화 — 요청마다 다시 읽을 필요 없는 값이라 런타임 read/write용인 `kv`/`config/*` 대신 flat file 선택. **이 키가 바뀌면 기존에 구독된 브라우저 전부가 조용히 깨짐**(구독이 최초 구독 시점 공개키에 암호학적으로 묶여있음)이라 절대 재생성되면 안 되는 값 — `save/`는 배포(`git pull`+`pnpm build`)에도 안 건드려짐이 이미 검증된 전제
  - **신규 모듈 `server/node/pushApi.cjs`**: `rl_push_subscriptions` 테이블(유저당 여러 구독 가능, endpoint가 유니크 키라 재구독은 upsert). `GET /api/push/vapid-public-key`/`POST /api/push/subscribe`/`DELETE /api/push/subscribe`(멱등) 세 엔드포인트, 전부 `resolveUserId` 게이트. `sendPushToUser(userId, payload)` export — 유저의 구독 전부에 발송, `err.statusCode`가 404/410(만료/철회)이면 그 구독만 삭제, 그 외 에러는 로그만(구독 유지). **`web-push`의 `setVapidDetails()`는 설정한 키를 다시 읽을 방법을 제공 안 함** — `pushApi.cjs`가 `configureVapid()`로 공개키를 자체 모듈 변수에 캐싱해서 `GET /api/push/vapid-public-key`가 그걸 반환하도록 함
  - **트리거는 durable buffering의 ack 계약을 그대로 재사용**(새 신호 체계 안 만듦) — `runDurableProxyPump`(`server.cjs`)가 `finishPendingGeneration({status:'done'})` 성공 직후 `setTimeout(PUSH_NOTIFY_DELAY_MS=8초 기본값)`을 걸어서, 그 시점에도 `rl_pending_generations` 행이 여전히 존재하면(=탭이 라이브로 못 받아가서 ack 안 됨) 그때만 `sendPushToUser` 호출. 라이브로 보고 있던 정상 케이스는 스트림 종료 직후 거의 즉시 ack되므로 타이머가 발화할 때쯤엔 이미 행이 없어서 푸시 안 뜸. **절대 await 안 함**(타이머 발화 시점엔 이미 `res`가 끝난 지 한참 됨, 요청 경로에 영향 없어야 함). 푸시 문구는 항상 제네릭("답변이 도착했어요. 앱으로 돌아와 확인해보세요.") — 서버가 AI 응답 내용을 절대 안 보는 durable buffering 설계 원칙을 여기서도 유지(스니펫 텍스트 없음)
  - **클라이언트 구독 흐름**: `NotificationToggle.svelte`(기존 알림 on/off 토글) 확장 — 권한 승인 후 `registration.pushManager.subscribe()` → `POST /api/push/subscribe`. **구독 실패해도 `DBState.db.notification`은 원복 안 함**(인앱 알림은 푸시 구독 성공 여부와 무관하게 계속 동작, 권한 거부일 때만 기존처럼 원복). 끌 때는 best-effort로 `pushManager.getSubscription()`→`unsubscribe()`+서버에 삭제 요청. `NodeStorage`에 `getVapidPublicKey`/`subscribePush`/`unsubscribePush` 3개 메서드 추가(이번 세션에 pending-generation 클라이언트 호출들을 `NodeStorage` 경유로 배선한 것과 같은 이유 — 401 재시도 로직 재사용)
  - **`public/sw.js`에 `push`/`notificationclick` 리스너 추가**: 알림 클릭 시 이미 열린 창이 있으면 focus, 없으면 `clients.openWindow('/')`. **v1 한계**: 특정 채팅으로 딥링크 안 함(앱 루트만 열림) — 이 앱(라우터 없는 3-엔트리 반MPA)엔 애초에 URL로 특정 채팅을 여는 메커니즘 자체가 없어서 범위 밖으로 남김
  - **범위 경계**: durable buffering이 이미 커버 안 하는 요청(CORS 우회 직접 성공, Kobold/Ollama 등)은 `rl_pending_generations` 행 자체가 안 생겨서 푸시도 절대 안 뜸 — 새 갭이 아니라 기존 갭 재사용
  - **검증**: 실제 서버 기동 후 `save/__vapid_public_key`/`__vapid_private_key` 생성과 `GET /api/push/vapid-public-key` 응답 확인. 진짜 P-256 커브 위의 유효한 공개키로 만든 가짜(존재 안 하는) FCM 구독을 등록 → `sendPushToUser` 직접 호출 → 실제로 404 응답을 받고 그 구독 행이 삭제되는 것 확인(반대로 커브에 안 맞는 진짜 무효 키는 삭제 안 되고 로그만 남는 것도 확인, 구분 로직 검증). **핵심 시나리오 재현**: mock SSE 업스트림으로 durable 요청을 보내고 **ack 안 한 채로 방치** → `PUSH_NOTIFY_DELAY_MS`를 1.5초로 낮춰서 실제로 그 시점에 `sendPushToUser`가 불려서(가짜 구독이 404로 정리됨) 발동한 것 확인. **반대 케이스**: 같은 요청을 완료 즉시 ack하면 같은 대기시간이 지나도 구독이 안 건드려지는 것(=푸시 안 뜸) 확인 — 오탐 없음 검증. `svelte-check` 0 에러, `vite build` 정상 완료(`dist/sw.js`에 새 리스너 포함 확인)
- durable buffering 도입 후 "응답이 안 오고 로딩 원이 계속 도는" 버그 수정 (`pendingGenRecovery.ts`) — durable buffering(위 항목) 자체는 정상 동작했지만, 그 복구 로직에 있던 dedup 오판이 원인이었음
  - **원인**: `sendChat()`(`index.svelte.ts`)은 스트림이 시작되기도 전에 **빈 placeholder 메시지**(`data:''`)를 낙관적으로 먼저 push하면서 `chatId`에 이번 generationId를 그대로 찍음. `fetchViaProxy2`(`globalApi.svelte.ts`)는 순수 `fetch()`라 클라이언트 쪽엔 스톨 타임아웃이 전혀 없어서, 연결이 에러 없이 그냥 멈추면(네트워크 완전 단절이 아니라 "조용히 멈춤" — 모바일 백그라운드 탭이 커넥션을 열어둔 채 멈추는 경우 등) `reader.read()`가 영원히 pending 상태로 남아 `finally` 블록(스트림 루프 종료 처리, `isStreaming=false`/`doingChat=false`/ack)이 절대 안 불림. 이 상태에서 서버는 durable pump로 완주해서 `rl_pending_generations`가 `status='done'`이 되고, 탭이 포그라운드로 돌아오면 `checkPendingGenerationsForChat`이 도는데, 기존 dedup 체크(`chat.message.some(m => m.chatId === row.id)`)가 "chatId만 같으면 이미 전달된 것"으로 오판 — 그 빈 placeholder에 매칭되어 서버의 완성된 응답을 내용은 채우지도 않고 그냥 ack만 하고 버림. 결과: 빈 말풍선 + `isStreaming`/`doingChat` 영구 true(로딩 원 무한 회전)
  - **수정**: dedup 판단 기준을 "메시지 존재 여부"에서 **`chat.isStreaming === false`**(라이브 경로가 실제로 끝까지 갔다는 유일하게 신뢰 가능한 신호)로 교체. `isStreaming`이 아직 true인 채로 매칭되는 메시지가 있으면(placeholder거나 스톨 전 일부 청크만 받은 상태) 그 메시지 자리에 replay한 전체 텍스트를 **덮어쓰고**(새 메시지로 push 안 함, 중복 방지), `chat.isStreaming`을 false로, 복구 대상이 현재 보고 있는 채팅이면(`selectedCharID`+`chatPage` 일치) 전역 `doingChat`(로딩 원 게이팅 store, `process/index.svelte.ts`)도 false로 내림. `doingChat`은 기존 프로젝트 관례대로 순환참조 방지를 위해 동적 `import()`로 가져옴
  - **검증**: 실기기 재현 대신(에이전트는 실제 네트워크/모바일 기기에 접근 불가) 실제 빌드(`pnpm build`) + 스크래치 DB로 `server.cjs` 직접 기동 + Chrome 브라우저 자동화로 상태 기반 통합 테스트 수행 — `bootstrap.ts`에 임시 디버그 훅(`window.__debugDBState`/`__debugCheckPendingGen`/`__debugDoingChat`, 검증 후 원복하고 커밋 안 함)을 걸어 실제 캐릭터/채팅에 정확히 문제 상황(빈 placeholder + `isStreaming=true` + `doingChat=true`)을 만들고, `rl_pending_generations`에 실제 OpenAI 호환 SSE 바이트를 직접 심어 recovery를 호출. 결과: (1) 메시지 없는 정상 케이스는 그대로 새 메시지로 추가됨(기존 동작 유지), (2) **stuck placeholder는 제자리에서 덮어써지고 로딩 원(전송 버튼 아이콘)이 실제로 정상으로 돌아옴**(스크린샷으로 확인) — 이번에 고친 핵심 시나리오, (3) 이미 정상 전달된 메시지(`isStreaming=false`)는 같은 chatId로 다시 done 행을 심어도 안 건드리고 스킵 — 회귀 없음. `rl_pending_generations` 행은 두 케이스 모두 정상적으로 ack(삭제)됨도 SQLite로 확인. `svelte-check` 0 에러
  - **[후속] 위 수정을 실서비스에 배포한 뒤에도 특정 유저(Vertex AI 계열 ModelPreset 사용)는 여전히 durable 경로 자체가 전혀 안 탐** — 배포 후 SSH로 프로덕션 로그/DB를 직접 대조하며 재조사(`rl_pending_generations` 계속 비어있음, `/proxy2`로 가는 durable 헤더 자체가 안 보임, nginx 접근 로그엔 실제 AI 응답 호출인 `POST /proxy2`가 아예 한 번도 안 잡힘 — `POST /api/token/refresh`(서비스어카운트 OAuth 토큰 교환)만 서버를 거치고 실제 `generateContent` 호출은 폰에서 Google로 직접 나감)
    - **진짜 원인**: `fetchNative`(`globalApi.svelte.ts`)는 "직접 브라우저→프로바이더 fetch 먼저 시도 → 실패하면(CORS 등) `/proxy2`로 폴백"하는 구조인데, Google Vertex AI의 `aiplatform.googleapis.com`가 CORS를 허용해서 직접 fetch가 항상 성공함 → durable buffering이 유일하게 개입 가능한 `/proxy2` 자체를 절대 안 탐. 이건 durable buffering 설계 당시 이미 "알려진 한계"로 문서화돼 있던 항목(#1: CORS 허용 커스텀 엔드포인트는 사정권 밖)이 실제로 발현된 사례 — 새로운 버그가 아니라 문서화된 한계가 처음으로 실사용자에게 노출된 것
    - **수정**: `fetchNative`에서 `arg.durable?.parserKind`가 있으면(=메인 어시스턴트 응답 스트리밍 호출) 직접 fetch 시도 자체를 건너뛰고 곧장 `fetchViaProxy2`로 보내도록 분기 추가 — durable 보호가 필요한 요청이라면 애초에 서버를 거쳐야 의미가 있으므로, "빠른 직접 경로" 최적화보다 신뢰성 보장을 우선시함. `window.userScriptFetch`/`useLocalNetworkRoute` 분기보다도 앞서 배치해서 모든 우회 경로보다 우선 적용됨
    - **함께 발견했지만 고치지 않은 것(레거시 모델 시스템의 별도 구멍)**: `google.ts`의 non-streaming 레거시 Vertex/GoogleCloud 호출부(`requestGoogle`)는 `fetchNative`에 애초에 `durable`을 안 넘겨서 통과 자체가 안 됨 — 파라미터 하나 추가해서 시도해봤지만, 재생(replay) 쪽 파서(`getTranStream`)가 SSE 형식(`data: ...`)만 파싱하게 짜여있어서 non-streaming 순수 JSON 응답을 넣으면 에러 없이 **빈 텍스트로 복구**되는 새 실패 모드가 생기는 걸 확인하고 되돌림(서버 캐치는 되는데 클라이언트 재생이 깨지는 반쪽짜리 수정이라 위험 판단). `anthropic.ts`의 non-streaming 분기(`requestClaudeHTTP`)도 스트리밍 분기와 달리 `fetchNative`가 아니라 완전히 다른 함수 `globalFetch`를 써서 durable을 원천적으로 지원 안 함 — 이쪽은 함수 자체를 바꿔야 하는 더 큰 변경이라 손 안 댐. 둘 다 **레거시 모델 시스템(ModelPreset이 아닌 구버전 "채팅 봇" 설정)의 non-streaming 조합에서만** 발생하고, 이번에 실제로 겪은 사용자(ModelPreset 경로)와는 무관 — 다음에 그 조합으로 문제가 재현되면 참고
    - **검증**: 로컬에서 CORS를 허용하는(`Access-Control-Allow-Origin: *`) mock OpenAI 호환 서버를 띄우고, 실제 빌드+스크래치 서버+Chrome으로 "Custom API" 리버스 프록시를 그 mock에 연결해 스트리밍 메시지 전송 → mock 서버 로그에 `origin=(none) via=proxy(x-forwarded-for present)`로 정확히 한 번만 찍힌 것으로 확인(수정 전이었다면 브라우저 Origin 헤더가 찍히는 직접 요청이었을 것 — 실제로 non-streaming 레거시 테스트에서는 스트리밍 켜기 전 실수로 그 케이스가 그대로 재현되어 `origin=http://localhost:6056`으로 직접 요청이 찍히는 것도 대조군으로 확인함). 진단용으로 잠깐 추가했던 서버 로그(`[DurableDiag]`, `server.cjs`)는 원인 확정 후 제거. `svelte-check` 0 에러, `node --check server.cjs` 통과
  - **[후속 조사] 위 수정을 배포한 뒤에도 특정 유저는 여전히 증상 재현 — 여러 라운드에 걸친 프로덕션 실기기 조사 끝에 진짜 원인은 위 두 항목과 별개로 "플러그인 경유 연결"이었음이 확정됨.** 조사 도구: SSH로 프로덕션 SQLite(`rl_pending_generations`)/nginx 접근 로그/PM2 로그를 직접 대조. 중간에 `logger.info()`가 stdout이 아니라 앱 자체 SQLite(`save/logs.db`)에만 쌓인다는 것도 뒤늦게 발견(`logger.error`/`warn`만 콘솔에도 씀) — 그래서 초반 진단 로그가 pm2 로그엔 하나도 안 잡혔던 것
    - **최종 원인**: 해당 유저의 캐릭터는 RisuAI 내장 요청 파이프라인이 아니라 **플러그인을 통해 Vertex AI에 연결**되어 있었음. 플러그인 API가 노출하는 `nativeFetch(url, options)`(`plugins.svelte.ts`가 그대로 `fetchNative`에 연결)는 `RequestInit`만 받고 `durable`/`chatId` 개념 자체가 없음 — 플러그인이 이 API로 요청을 보내면 `arg.durable`이 애초에 undefined라, 위에서 고친 `fetchNative`의 direct-fetch 우회 방지 분기(`arg.durable?.parserKind` 체크)가 항상 false로 스킵됨. 플러그인 자체의 재시도/전략 체인(에러 메시지에 `[CPM]`/"getch strategies"/"Google-nativeFetch" 같은 문구로 노출됨, PocketRisu 코드베이스엔 이 문자열이 전혀 없음 — 전량 grep으로 확인)이 매번 다른 전략을 타면서 "가끔은 직접 fetch 성공(→ `/proxy2` 자체를 안 탐)", "가끔은 `/proxy2`는 타는데 durable 헤더 없음(→ 예전 `pipeline()` 경로, `ERR_STREAM_PREMATURE_CLOSE`)"처럼 들쭉날쭉하게 보였던 것 — 전부 이걸로 설명됨
    - **결론**: durable buffering은 설계상 RisuAI 자체 요청 파이프라인의 메인 어시스턴트 응답 호출 한 곳만 커버 — 플러그인이 대신 처리하는 요청은 애초에 사정권 밖. 새로 생긴 버그가 아니라 이 기능이 원래 손댈 수 없는 영역이 실사용자에게 노출된 것
    - **디버깅에 썼던 임시 코드는 전부 원복함**: `server.cjs`의 2차 진단 로그(`[DurableDiag2]`), `vite.config.ts`/`vite-env.d.ts`/`database.svelte.ts`/`SystemSettings.svelte`에 잠깐 추가했던 빌드 타임스탬프 표시(설정 > 시스템 탭에 실기기가 최신 배포를 받았는지 확인하려고 넣었던 것) 모두 제거 완료

  - **현재 상태 요약** (이 조사 세션 전체의 최종 정리)
    - **해결 완료 (머지됨)**:
      1. `pendingGenRecovery.ts` dedup 오판 수정 (`isStreaming` 기준으로 교체)
      2. `fetchNative`가 durable 태그된 요청도 직접 fetch로 먼저 시도해버리던 것 방지 (CORS 허용 엔드포인트에서도 `/proxy2` 강제)
      3. SW 위생 패치 — `sw.js`에 `skipWaiting`/`clients.claim` 추가(새 SW가 즉시 활성화되도록; 다만 이 SW는 애초에 앱 JS/HTML을 안 건드리므로 이번 증상 자체와는 무관)
    - **미해결 — 플러그인 구조 리팩 때 같이 처리**: 플러그인 경유 요청이 `/proxy2`(정확히는 durable 보호)를 안 타는 것으로 확정. 확인 방법: 플러그인 비활성화 상태에서 비행기 모드 재현 테스트 → 정상 복구되면 확정. 근본 해결은 플러그인 fetch 경로 자체를 프록시/durable 경유로 통합하는 더 큰 작업(플러그인 API에 `durable` 옵션 노출 여부 포함) 필요 — 지금은 안 건드림
    - **미해결 — 별도 버그, 현재 계정엔 영향 없음**: `google.ts` 레거시 non-streaming 경로(`durable` 미전달, 고치면 재생 파서가 SSE 전용이라 빈 텍스트 복구되는 새 실패모드 생김 — 반쪽 수정 위험해서 보류), `anthropic.ts` 레거시 non-streaming 경로(`fetchNative` 대신 `globalFetch`를 써서 durable 자체가 구조적으로 불가능 — 함수 교체가 필요한 더 큰 변경이라 보류). 둘 다 "모델 프리셋 고정"이 아닌 레거시 모델 시스템에서만 발생 — 이번 계정은 프리셋 고정이라 무관
    - **확인해서 배제한 가설**: 푸시 인프라 자체 문제(직접 테스트 발송 정상 도달 확인), 구독 미등록(`rl_push_subscriptions` DB 확인), 새 채팅이 레거시 모델로 새는 것(계정이 "모델 프리셋 고정"이라 애초에 불가능), SW가 앱 JS를 캐싱해서 구버전을 서빙하는 것(SW `fetch` 핸들러가 `/sw/*`·`/tf/*`만 가로채고 일반 페이지/자산 요청은 안 건드림 — 그쪽은 서버 `Cache-Control`이 이미 올바르게 처리 중: HTML `maxAge:0`, 해시된 JS `1y`)

## 제약 사항
- src/ts 함수 시그니처 변경 시 upstream 호환성 고려
- 채팅 메시지 렌더링 DOM 구조/클래스명 변경 금지 (커스텀 CSS 호환)
- UI 변경은 자유, 단 src/ts 함수 호출은 유지
- pm2 전체 경로 사용: /usr/bin/pm2
- ecosystem.config.cjs (ES module 프로젝트라 .cjs 필수)
- upstream의 서버 사이드 생성 기능을 cherry-pick할 경우, 서버 완성 경로가 chatApi.cjs의 upsertChatFull()도 호출하는지 반드시 확인할 것 (미호출 시 서버 생성 메시지가 rl_chats에 반영되지 않아 클라이언트 재접속 후 덮어씌워질 수 있음)
